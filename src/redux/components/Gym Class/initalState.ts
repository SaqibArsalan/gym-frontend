// import { getFromSession } from 'utils/Cache';
import {IGymClassInitialState} from './GymClass.interface';

const classInitialState: IGymClassInitialState = {
	classList: [],
	classDetails: null,
	classListForDropdown: []
};

export default classInitialState;
